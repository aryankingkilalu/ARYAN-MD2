name: ∆RY∆N-TECH-𝚾𝚳𝐃
services:
  - name: ∆RY∆N-TECH-𝚾𝚳𝐃-service
    type: web
    ports:
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin
      
